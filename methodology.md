# 轻量级企业迭代开发方法论（Lean Enterprise Iteration）

> 适用于个人/小团队产品开发的工程方法论。继承 Agile 精神，融合企业级质量门禁与可追溯性，同时保持轻量——每个仪式都有实际价值，不为流程而流程。

当用户表达以下意图时启用本 Skill：增加功能、增加业务场景、收到新需求、扩展能力、修改现有行为、调整数据结构、新增命令等。

---

## 核心哲学

五条核心价值观贯穿整个迭代生命周期，所有决策和设计以此为锚：

| 原则 | 含义 | 反模式 |
|------|------|--------|
| **最小变更** | 只改受影响的部分，不做无关重构 | "顺便重构一下"的冲动 |
| **数据安全** | 逻辑删除原则不可破坏，永不物理删除 | `DELETE` 语句、`rm` 数据文件 |
| **向后兼容** | 新增字段有默认值，不破坏已有数据结构 | 新增必填字段却不给默认值 |
| **一致性** | 定义层 → 数据层 → 代码层 → 测试层 四层同步 | 改了代码忘了更新文档 |
| **可追溯** | 需求 → 场景 → Story → 代码 → 测试 → 发布 全链路可追踪 | "这个改动对应哪个需求来着？" |

---

## 项目上下文

项目完整信息见项目根目录 `CLAUDE.md`。开发前先读取该文件了解最新项目状态，包括但不限于：

- 项目路径与技术栈
- 数据模型与字段定义
- CLI 命令参考或 API 文档
- 编码规范与设计约束

**适配要求**：采用本方法论的项目，`CLAUDE.md` 应至少包含以下内容，确保规划Agent 和交付Agent 能独立获取上下文：

1. **项目路径** — 项目根目录的绝对路径
2. **目录结构** — 当前项目的目录树概览（可简化）
3. **技术栈** — 编程语言、运行环境、依赖约束
4. **数据模型** — 核心实体、字段、关联关系
5. **关键规则** — 业务规则、设计约束、禁止事项
6. **命令/API 参考** — 可执行命令或 API 端点列表

---

## 标准目录结构

本方法论推荐以下目录结构。首次使用前，确保所有目录均已创建：

```
<project>/                           # 项目根目录
│
├── define/                         # 领域定义（数据结构、枚举、ADR）
│   ├── <entity>.md                 #   各实体字段定义
│   ├── enum-dictionary.md          #   枚举值字典
│   └── adr/                        #   架构决策记录（ADR）
│
├── data/                           # 实际数据（json / yaml / 等）
│   ├── <entity>.json               #   业务数据文件
│   ├── enum-dictionary.json        #   枚举值（预填）
│   └── .backup/                    #   数据备份（回滚用，实施前创建）
│
├── src/ 或 scripts/                # 源代码
│   └── <entry-point>               #   项目入口（CLI / API / 等）
│
├── requirements/                   # 需求文档
│   ├── requirements.md             #   需求定稿（含修订记录）
│   └── scenario.md                 #   业务场景 + 运维场景
│
├── change-log/                     # 变更记录（每版本一个文件）
│
├── versions/                       # 产品版本路线图与发布说明
│   ├── roadmap.md                  #   版本路线图（所有版本概览）
│   └── v{x.y.z}.md                 #   各版本发布说明
│
├── plan/                           # 版本实施计划
│   ├── to-do/                      #   待实施的 Story Plan
│   │   └── v{N}/                   #     按版本组织（v1, v2, ...）
│   └── done/                       #   已完成的 Story Plan
│       └── v{N}/
│
├── testing/                        # 测试
│   ├── test-case.md                #   测试用例定义
│   ├── test-runner.<ext>           #   自动化测试脚本
│   └── test-report/                #   测试报告
│
├── sandbox/                        # 测试沙箱（临时，每次测试前重建）
│
├── .claude/                        # Claude Code 配置
│   └── settings.json               #   项目级权限与配置
│
└── CLAUDE.md                       # 项目上下文（数据模型、命令参考等）
```

> 注：上述目录名和文件名均为推荐约定。项目可根据实际情况调整，但应在 `CLAUDE.md` 中明确实际使用的目录结构。

### 目录初始化（自动执行）

**本 Skill 被激活后，在进入任何阶段之前，必须先执行以下目录初始化命令。** 缺失目录会阻断后续流程（如阶段 3 无法输出 Plan 文件、阶段 4 无法备份数据）。

```bash
mkdir -p define/adr data/.backup requirements change-log versions plan/to-do plan/done testing/test-report sandbox
```

执行规则：
- 上述命令在项目根目录执行，幂等，可重复执行
- 必须在读取 `CLAUDE.md` 之后、进入阶段 1 之前完成
- 若任一目录创建失败（如权限不足），中止流程并告知用户
- 如项目使用不同目录结构（如 `src/` 替代 `scripts/`），按 `CLAUDE.md` 中的实际结构调整

### 目录与阶段的映射关系

| 阶段 | Agent | 读取目录 | 写入/产出目录 |
|------|-------|---------|-------------|
| 阶段 1: 需求分析 | 规划 | `requirements/`、`testing/` | `requirements/`、`testing/` |
| 阶段 2: 方案设计 | 规划 | `define/`、`data/`、源码目录 | `define/`（含 `adr/`）、`data/enum-dictionary.json` |
| 阶段 3: 版本计划 | 规划 | `requirements/`、`testing/`、`versions/` | `plan/to-do/`、`versions/roadmap.md`、`versions/v{x.y.z}.md` |
| 阶段 4: 实施验证 | 交付 | `plan/to-do/`、`define/`、源码目录、`data/` | 源码目录、`data/`、`data/.backup/`、`plan/done/` |
| 阶段 5: 记录发布 | 交付 | `CLAUDE.md`、`change-log/`、`versions/` | `CLAUDE.md`、`change-log/`、`versions/v{x.y.z}.md`（更新） |

### 版本号管理规则

采用语义化版本号（Semantic Versioning）：`MAJOR.MINOR.PATCH`（如 `1.2.0`）

| 版本位 | 递增条件 | 示例 |
|--------|---------|------|
| **MAJOR** | 不兼容的破坏性变更（数据格式变化、命令重命名、字段删除） | `1.2.0` → `2.0.0` |
| **MINOR** | 向后兼容的新功能（新增字段、新增命令、新增实体） | `1.2.0` → `1.3.0` |
| **PATCH** | 向后兼容的缺陷修复（Bug fix、错误信息优化、性能改进） | `1.2.0` → `1.2.1` |

附加规则：
- 初始版本号为 `1.0.0`
- 一个 Release Version 对应一次版本号递增
- 若同一次迭代中拆分多个 Release Version，每个独立递增版本号
- MAJOR 递增时，MINOR 和 PATCH 归零；MINOR 递增时，PATCH 归零
- 版本号由阶段 3 分配，阶段 5 确认后正式生效

---

## 全生命周期概览

整个迭代分为 **5 个阶段**，含 **5 道门禁**。阶段 1-3 由规划Agent负责，阶段 4-5 由交付Agent负责，两者通过 `plan/to-do/` 队列解耦，可并行推进。

### 规划Agent 流水线（阶段 1-3）

```
需求输入
  │
  ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段 1: 需求分析                                             │
│  ├─ 需求澄清 → 分类 → 风险识别 → 验收标准定义                    │
│  └─ 产出: requirements.md 更新、scenario.md 更新、测试用例更新    │
└────────────────────────────┬────────────────────────────────┘
                             │
                     🔒 门禁 1: 需求确认
                     （用户确认需求内容+成功标准）
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段 2: 解决方案设计                                         │
│  ├─ define/ → data/ → src/ 逐层设计                           │
│  ├─ 影响分析 → ADR（如需）                                     │
│  └─ 产出: 详细设计方案、受影响文件清单                            │
└────────────────────────────┬────────────────────────────────┘
                             │
                     🔒 门禁 2: 方案确认
                     （用户确认方案+影响范围）
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段 3: 版本计划                                             │
│  ├─ Goal → Epic → Story 拆解                                  │
│  ├─ 估算 → 依赖分析 → 版本划分                                  │
│  └─ 产出: Plan 文件（plan/to-do/v{N}/）                        │
└────────────────────────────┬────────────────────────────────┘
                             │
                     🔒 门禁 3: 计划确认
                     （用户确认拆分+版本划分）
                             │
                             ▼
              ┌──────────────────────────┐
              │    plan/to-do/v{N}/      │
              │    (交接队列 — 解耦点)     │
              └────────────┬─────────────┘
                           │
              ── 规划Agent 可继续准备下一版本 ──▶
```

### 交付Agent 流水线（阶段 4-5，按版本循环）

```
              ┌──────────────────────────┐
              │    plan/to-do/v{N}/      │
              │    (拉取待实施版本)         │
              └────────────┬─────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段 4: 实施与验证（按版本循环）                                │
│  ├─ 冲突检查 → 执行排序 → 逐个 Story 实施+定向验证               │
│  └─ 产出: 代码变更、测试通过                                    │
└────────────────────────────┬────────────────────────────────┘
                             │
                     🔒 门禁 4: 质量确认
                     （冒烟测试全绿 + 代码审查通过）
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段 5: 记录与发布（按版本循环）                                │
│  ├─ CLAUDE.md 更新 → change-log → Git commit                  │
│  └─ 产出: 可追溯的版本记录                                      │
└────────────────────────────┬────────────────────────────────┘
                             │
                     🔒 门禁 5: 发布确认
                     （文档同步 + Git 提交完整）
                             │
                             ▼
                  ✅ 版本发布完成，继续下一版本
                           │
                           └──── 循环：拉取 plan/to-do/ 中下一版本 ──▶
```

详细流程定义在本 Skill 目录的以下文件中：

| 覆盖阶段 | Skill 文件 | 归属 Agent | 职责 |
|---------|-----------|-----------|------|
| 阶段 1-2 | `planner/analysis-and-design.md` | 规划Agent | 需求澄清 → 解决方案设计 |
| 阶段 3 | `planner/version-planning.md` | 规划Agent | Goal→Epic→Story 拆分 + Release Version 划分 |
| 阶段 4-5 | `deliverer/implementation.md` | 交付Agent | 从 `plan/to-do/` 拉取版本，按 Story 实施、验证、记录、Git 提交 |

> 以上文件位于本 Skill 目录下的 `planner/` 和 `deliverer/` 子目录中，与 `methodology.md` 位于同级。规划Agent 和交付Agent 分别加载对应文件执行阶段任务。

---

## 双Agent协作模型

传统串行流程（需求→方案→计划→实施→发布）在实际产品开发中存在瓶颈：规划和实施互相阻塞。引入 AI 后，可将"计划"与"实施"解耦为两个独立 Agent，通过 `plan/to-do/` 队列异步协作。

### 架构

```
规划Agent (Planning)                    交付Agent (Implementation)
  阶段 1-3                                 阶段 4-5
  ┌──────────────┐                       ┌──────────────┐
  │ 需求分析      │                       │              │
  │ 方案设计      │─── plan/to-do/ ──────▶│ 拉取版本计划  │
  │ 版本计划      │     (交接队列)         │ 实施验证      │
  │              │                       │ 记录发布      │
  │ 产出:        │                       │              │
  │ define/      │                       │ 产出:        │
  │ requirements/│                       │ src/         │
  │ testing/     │                       │ data/        │
  │ plan/to-do/  │                       │ Git commit   │
  └──────────────┘                       └──────────────┘
       ▲                                       ▲
       │                                       │
  门禁 1-3                                门禁 4-5
```

### 职责边界

| 维度 | 规划Agent | 交付Agent |
|------|----------|----------|
| **范围** | 阶段 1-3（需求分析、方案设计、版本计划） | 阶段 4-5（实施验证、记录发布） |
| **可写** | `define/`、`requirements/`、`testing/`、`plan/to-do/`、`versions/`、`data/enum-dictionary.json`（枚举字典） | 源码目录、`data/`（业务数据）、`data/.backup/`、`plan/done/`、`change-log/`、`CLAUDE.md` |
| **只读** | 源码目录、`data/<业务数据文件>`（作为现状参考） | `define/`、`requirements/`、`testing/`（作为实施依据） |
| **禁止碰** | 源码目录的代码实现；`data/` 中业务数据的修改 | `define/` 的数据结构定义；`requirements/` 的需求文档 |
| **门禁** | 门禁 1、2、3 | 门禁 4、5 |

### 协作模式

- **规划先行**：规划Agent 提前完成阶段 1-3，将 Plan 文件写入 `plan/to-do/v{N}/`，不等待交付Agent
- **异步交付**：交付Agent 从 `plan/to-do/` 拉取待实施版本，按 Story 逐个实施，完成后移动到 `plan/done/`
- **并行推进**：交付Agent 实施 v1 的同时，规划Agent 可以准备 v2 甚至 v3 的计划
- **队列驱动**：`plan/to-do/` 是唯一交接面，规划Agent 只负责写，交付Agent 只负责读+消费

### 跨Agent一致性保证

定义层 → 数据层 → 代码层 → 测试层 四层在双Agent模型下被拆分到两个 Agent 手中。一致性通过 `plan/to-do/` 中的 Plan 文件作为**契约**来保证：

- **规划Agent** 在 Plan 文件中明确定义变更目标：数据结构、字段、枚举值、测试用例编号
- **交付Agent** 按 Plan 文件的契约实施代码和数据层，不自行偏离
- 交付Agent 实施时发现 Plan 无法落地，应暂停并反馈规划Agent 修订后继续

### 版本号分配

版本号由规划Agent在阶段3分配（见语义化版本规则），交付Agent直接使用，不自行修改。若实施中发现需要调整版本号（如发现额外变更导致破坏性升级），交付Agent应与规划Agent沟通确认。

### 启动方式

两个 Agent 通过独立的 Claude Code 会话并行运行：

1. **规划Agent**：用户说明新需求，Skill `agile-dev` 自动激活，从阶段 1 推进到阶段 3，产出 Plan 文件
2. **交付Agent**：用户指定要交付的版本号，Skill `agile-dev` 自动激活，从阶段 4 开始消费 `plan/to-do/`

> 实际操作：开两个终端窗口，一个跑规划、一个跑交付，互不阻塞。

---

## 角色定义

即便是单人项目，也需在迭代过程中切换以下角色视角，确保多维度思考。两个 Agent 可在独立会话中并行运行。

### 职能角色

在 Agent 内部，仍需切换以下职能视角：

| 角色 | 职责 | 在哪个阶段重点出现 |
|------|------|-------------------|
| **产品负责人（PO）** | 定义需求、排优先级、验收结果 | 阶段 1、3（规划Agent） |
| **开发者（Dev）** | 方案设计、编码实现 | 阶段 2（规划Agent）、4（交付Agent） |
| **质量保证（QA）** | 定义测试用例、执行验证、质量门禁 | 阶段 1（定义用例）、4（执行验证） |
| **运维（Ops）** | 数据安全、向后兼容、发布记录 | 阶段 4、5（交付Agent） |

> 角色冲突警示：当同一个决策从 PO 视角看合理，但从 Dev 视角看有隐患时，必须优先考虑数据安全和向后兼容原则。

---

## 贯穿标准（Cross-Cutting）

以下标准贯穿所有阶段，各子文件不再重复声明。实施时以此为唯一标准。

### 编码规范

编码规范由各项目在根目录 `CLAUDE.md` 中定义。本方法论不硬编码特定技术栈规则。

项目 `CLAUDE.md` 应至少声明：
- **编程语言与版本** — 如 Python 3.12+、Node.js 20+ 等
- **依赖约束** — 如"纯标准库"、"允许的第三方包"等
- **入口文件** — 项目的主入口路径
- **命名规范** — 字段命名、函数命名、文件命名约定
- **输出格式** — 用户界面的语言和格式约定
- **错误处理** — 错误信息的格式和退出方式

### 设计原则

- **最小变更** — 只改受影响的部分，不做无关重构。一个 PR 解决一个问题
- **数据安全** — 逻辑删除原则不可破坏，不做物理删除。"删除"操作只改状态字段
- **向后兼容** — 新增字段必须有默认值，不破坏已有数据结构。存量数据能正常读取
- **一致性** — 定义层 → 数据层 → 代码层 → 测试层 四个层面保持同步，任一变更触发关联更新

### 测试策略

| 层级 | 说明 | 触发时机 |
|------|------|---------|
| **定向验证** | 执行当前 Story 涉及的测试用例（`testing/test-case.md` 中对应编号） | 每个 Story 实施完成后 |
| **冒烟测试** | 运行全量测试套件 | 每个 Release Version 的最后一个 Story 完成后 |
| **回归验证** | 用户手动验证受影响场景的实际行为 | 冒烟测试通过后 |

测试通过标准：
- 定向验证：受影响用例 **100% 通过**
- 冒烟测试：全量用例 **100% 通过**（0 失败，0 错误）
- 回归验证：至少一个正向场景和边界场景手动确认

---

## 定义完成（Definition of Done）

### Story 级 DoD

- [ ] 所有子任务完成
- [ ] 定向验证测试全部通过
- [ ] 代码符合 `CLAUDE.md` 定义的编码规范和设计原则
- [ ] Plan 文件已移动到 `plan/done/`

### Version 级 DoD

- [ ] 所有 Story 完成（各自满足 Story 级 DoD）
- [ ] 冒烟测试 100% 通过
- [ ] `CLAUDE.md` 已更新
- [ ] 变更记录已写入 `change-log/`
- [ ] Git commit 已创建，message 格式 `v{版本号}: {简短描述}`
- [ ] 轻量回顾已完成（见"持续改进"章节）

### Iteration 级 DoD

- [ ] 所有 Release Version 完成（各自满足 Version 级 DoD）
- [ ] `plan/to-do/` 中本轮迭代的版本已清空（全部移入 `plan/done/`），即所有交付物已完成
- [ ] 需求方验收通过（PO 角色确认）
- [ ] 技术债务已记录（如有，记入 `requirements/requirements.md`）
- [ ] 迭代回顾已完成

---

## 风险管理框架

### 风险等级

| 等级 | 含义 | 响应策略 |
|------|------|---------|
| 🔴 **高** | 可能导致数据丢失、不可逆操作 | 方案必须包含回滚计划；用户必须明确确认 |
| 🟡 **中** | 可能影响已有数据格式或已有功能行为 | 方案中标注缓解措施；加强回归测试 |
| 🟢 **低** | 纯新增功能，不影响已有模块 | 正常流程，无需额外措施 |

### 风险识别触发点

- **阶段 1**：识别需求本身的风险（数据迁移？破坏性变更？）
- **阶段 2**：识别方案实施的技术风险（影响面大？逻辑复杂？）
- **阶段 3**：识别版本编排风险（Story 间依赖冲突？容量过载？）
- **阶段 4**：识别实施中的意外风险（数据结构冲突？存量数据边界情况？）——发现后应反馈规划Agent，必要时修订 Plan

风险识别结果记录在对应 Plan 文件中，格式：`风险等级 | 风险描述 | 缓解措施`

---

## 可追溯矩阵

每个变更必须满足以下追溯链，任一环节不可缺失：

```
requirements/requirements.md（需求条目 + 修订记录）
  │  需求编号: REQ-YYYYMMDD-NN
  ▼
requirements/scenario.md（业务/运维场景）
  │  场景编号: B-NN（业务）/ O-NN（运维）
  ▼
testing/test-case.md（测试用例）
  │  用例编号: TC-BNN / TC-ONN（与场景编号对应）
  ▼
plan/to-do/v{N}/E{N}-S{N}-{描述}.md（实施计划）
  │  Story 中标注影响的场景编号和测试用例编号
  ▼
源码 + data/（代码变更 + 数据变更）
  │  Git commit message 包含版本号
  ▼
change-log/YYYY-MM-DD-v{x.y.z}-{描述}.md（变更记录）
  │
  ▼
versions/v{x.y.z}.md → versions/roadmap.md（版本发布说明 → 路线图汇总）
```

追溯检查：从任意一个环节向上下游追溯，应能定位到对应条目。

---

## 架构决策记录（ADR）

### 触发条件

满足以下任一条件时，必须在 `define/adr/` 目录下创建 ADR：

1. 引入新的设计模式或架构约束
2. 修改编号规则、校验逻辑等核心业务规则
3. 数据模型发生结构性变化（新增实体、字段语义变更）
4. 方案存在两个以上可行路径，需要记录选择理由

### ADR 模板

文件名：`define/adr/YYYY-MM-DD-{简短标题}.md`

```markdown
# ADR: {决策标题}

**日期**: YYYY-MM-DD
**状态**: 提议 / 已采纳 / 已废弃
**决策者**: {角色}

## 背景
{为什么需要做这个决策？上下文是什么？}

## 决策
{选择了什么方案？}

## 备选方案
| 方案 | 优点 | 缺点 |
|------|------|------|
| A: ... | ... | ... |
| B: ... | ... | ... |

## 影响
{这个决策影响了哪些部分？}
```

---

## 质量度量

每个 Release Version 完成后记录以下指标（写入 `change-log/` 变更记录）：

| 指标 | 计算方式 | 目标值 |
|------|---------|--------|
| 测试通过率 | 通过用例数 / 总用例数 | = 100% |
| 场景覆盖率 | 有测试用例覆盖的场景数 / 总场景数 | ≥ 90% |
| 变更失败率 | 需要回滚或紧急修复的版本数 / 总版本数 | = 0% |
| 定向验证效率 | Story 完成到定向验证通过的时间 | 同一次会话内 |

---

## 持续改进

### 轻量回顾（Retrospective）

每个 Release Version 的 Git commit 完成后，执行一次轻量回顾（5 分钟即可）：

1. **What went well?** — 本次迭代中哪个环节最顺畅？为什么？
2. **What was painful?** — 哪个环节遇到了摩擦？是否有工作绕过流程？
3. **What to change?** — 是否有流程步骤需要调整？是否需要更新本方法论？

回顾结论写入 `change-log/` 变更记录的末尾，格式：

```markdown
## 回顾
- ✅ {做得好的点}
- ⚠️ {摩擦点}
- 🔧 {改进建议}（如有）
```

若改进建议涉及方法论本身，**立即修订本方法论文件**，不让改进点流失。

---

## 异常流程

### Hotfix 流程

紧急修复（如数据错误修复、严重 Bug）可走简化流程，但不可绕过关键门禁：

1. **快速诊断** — 明确问题根因和修复范围
2. **跳过阶段 3** — 单个修复不需要 Goal→Epic→Story 拆分，无需写入 `plan/to-do/`
3. **保留阶段 1 核心** — 记录问题到 `requirements/requirements.md`，更新场景和测试用例（规划Agent 职责）
4. **保留阶段 4 核心** — 实施修复后必须通过定向验证 + 冒烟测试（交付Agent 职责）
5. **保留阶段 5** — 正常记录和提交
6. **事后回顾** — 在回顾中分析是否需要加强预防措施

> Hotfix 是双Agent协作模型的唯一例外：允许在单个会话中端到端完成（同时涉及规划侧和交付侧的文件修改），但必须在事后确保追溯链完整（需求记录、测试用例、变更记录、Git commit 全部到位）。

### 回滚策略

每个 Release Version 在阶段 3 的 Plan 文件中必须包含回滚方案：

- **代码回滚**：`git revert <commit_hash>` 即可
- **数据回滚**：数据文件在实施前备份到 `data/.backup/`，回滚时覆盖还原
- **回滚触发条件**：冒烟测试失败且 1 小时内无法修复

---

## 禁止事项

以下行为明确禁止，违反即视为流程违规：

1. ❌ 跳过阶段直接编码 — 无需求文档和方案直接改代码
2. ❌ 跳过测试直接提交 — 未通过定向验证就进入下一个 Story
3. ❌ 物理删除数据 — 任何情况下都使用逻辑删除
4. ❌ 违反项目依赖约束 — 如项目规定纯标准库，不得引入第三方依赖
5. ❌ 跨版本合并提交 — 一个 commit 只能属于一个 Release Version
6. ❌ 静默变更 — 修改已有行为不更新测试用例和文档
7. ❌ 规划Agent 直接修改代码 — 规划Agent 不碰源码目录的代码实现
8. ❌ 交付Agent 跳过 Plan 队列 — 必须从 `plan/to-do/` 拉取版本计划，不得绕过计划直接实施
9. ❌ 交付Agent 直接修改需求/定义 — 交付Agent 不修改 `define/`、`requirements/` 中的文档
