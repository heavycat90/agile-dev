# Review 准则

## SKILL review

**核心准则**：SKILL.md 的读者是用户（人），不是 Claude（AI）。每一行必须回答同一个问题——"这段话是写给谁的？"

系统实现细节（队列如何交接、目录如何初始化、门禁如何判定、文件路径、模板格式）放在 `references/` 下由 Agent 加载，用户不需要在入口文件中读到。

### 自查问题

在生成 SKILL.md 内容或提出修改建议之前，对每一段逐一过：

1. **这个章节必须留在 SKILL.md 吗？** — 删掉它，用户还能不能完成他想做的事？
2. **它的内容和 SKILL.md 的定位一致吗？** — 是面向用户给出行动指引，还是面向 Claude 给出实现指令？
3. **用户在什么场景下需要读到这段话？** — 如果他不需要主动知道，就不该出现在这里。
4. **这段信息是否已存在于 `references/` 中？** — 如果是，就是重复。SKILL.md 不应是 reference 的索引或摘要。
5. **引导用户去读 reference 文件是否合理？** — 用户不需要知道 reference 文件的存在。他只需要知道"说什么"，系统负责加载对应的 reference。

---

## methodology review

**核心准则**：methodology.md 是方法论总纲，只回答"是什么"和"边界在哪"，不回答"怎么做"。

章节内容被删掉后，方法论的核心规则是否仍然完整？如果是→该内容不属总纲，应下沉到 reference。

### 自查问题

对每个章节逐一过三个追问：

1. **有没有另一份文件已经在说这件事？** — 同一信息在 SKILL.md 或其他 reference 中已经出现，就是冗余。例如 Agent I/O 列表在 SKILL.md 和各 reference 中重复了三次→总纲只保留一张概要表。环境清单在目录树中已经可见→独立表格是冗余。

2. **这段话是"规则"还是"操作指令"？** — "禁止 dev→prod 直接合并"是规则。"`git checkout qa && git merge dev`"是操作指令。总纲保留前者，后者交给 Agent 执行时自行推导或下沉到 reference。

3. **删掉这段后，有没有文件在承载它？** — 删除之前先问：这段内容的唯一副本是不是就在这里？如果是，必须有另一个文件来接。先确认无家可归，再补文件。

### 补文件的标准

当确认某段内容需要从 methodology.md 移出且无其他文件承载时，三个问题决定去向：

1. **这段内容有没有明确的所有者？** — 归属单一 Agent（只有某个 Agent 需要执行）→ 追加到该 Agent 的 reference。跨 Agent 或无 Agent（不归任何一个 Agent，如 Skill 激活时的前置步骤）→ 新建独立文件。属于方法论本身的补充说明→恢复到 methodology.md。

2. **这段内容的读者是谁？** — 读者是某个 Agent（执行时需要查阅）→ 追加到 Agent reference。读者是项目初始化者（人在配置新项目时需要）→ 新建独立文件。读者是方法论使用者（人在理解规则时需要）→ 保留在 methodology.md。

3. **这段内容的体量够不够独立成文件？** — 一个段落且与已有文件主题一致→追加。一套完整的模板+流程+命令，足够独立构成操作指南→新建。一句话补充→恢复原位，不为一行字新建文件。

三个问题交叉验证：先问"谁执行"确定归属，再问"谁读"确定读者，最后看体量——三者一致则执行，冲突时以归属和读者优先。例如精炼触发条件→归属明确（需求分析Agent）、读者明确（Agent 执行时查阅）、体量小→追加到 `agent-analyst/requirements-analysis.md`。初始化流程→无 Agent 归属（Agent 启动前）、读者是人（项目初始化者）、体量够→新建 `foundations/project-init-template.md`。GitHub Issues 链接→归属方法论、读者是人、体量一句话→恢复原位。

---

## design-principles review

**核心准则**：governance/design-principles.md 是所有 Agent 的"宪法"——定义贯穿所有阶段的价值观与硬规则。内容必须是普适的（适用于所有 Agent），不应包含仅针对单个 Agent 的指导。每一条原则必须有对应的反模式，让 Agent 知道"什么算违规"。

### 自查问题

1. **这条规则是否对所有 Agent 都有约束力？** — 如果只约束某一个 Agent，应下沉到该 Agent 的 reference。例如"QA Agent 仅写 envs/qa/"是 QA Agent 专属规则，应只在 QA reference 中定义（design-principles 中沙箱隔离是普适规则，但每个 Agent 的读写边界由 methodology 职责边界表定义）。

2. **是否存在与 methodology.md 重复的陈述？** — methodology 定义规则"是什么"（如"禁止 dev→prod 直接合并"），design-principles 定义"怎么做才对"（如"沙箱之间物理隔离，禁止跨环境写入"）。同样的规则不应在两个文件中用不同措辞各说一遍——design-principles 聚焦在操作层面的正确/错误示范。

3. **规则是否可验证？** — 抽象的价值观陈述（如"最小变更"）必须配具体的反模式或检查标准。没有反模式的原则 = 无法执行的宣言。

4. **沙箱规则是否覆盖了所有环境/模式组合？** — 模式 A（目录隔离）和模式 B（分支隔离）的正确做法和禁止事项必须对称覆盖，不能 A 有 5 条、B 只有 3 条。

5. **测试策略描述是否与各 Agent 的测试职责一致？** — 测试策略定义"测什么、谁测、何时测、通过标准"，各 Agent reference 定义"怎么测"。通过标准在 design-principles 中是唯一权威，Agent reference 只能引用不能重定义。

---

## risk-and-exceptions review

**核心准则**：governance/risk-and-exceptions.md 是异常处理手册——覆盖正常流程之外的所有场景（风险识别、ADR、Hotfix、回滚、禁止事项）。每个异常流程必须有明确的触发条件、执行步骤和回退路径。读者是全体 Agent 和用户（人在遇到紧急情况时需要查阅此文件）。

### 自查问题

1. **每个异常流程是否完整？** — 格式必须为：触发条件 → 执行步骤 → 回退/恢复路径。缺少任一环节 = 流程不可执行。

2. **Hotfix 与快速通道的边界是否清晰？** — 两条路径都跳过版本计划，容易混淆。对比表必须覆盖所有差异维度：触发方、Agent 边界、需求记录时机、追溯链要求。

3. **禁止事项是否与 methodology.md 的职责边界表一致？** — 如果禁止事项写"A 不能做 X"，但 methodology 职责边界表中 A 的"禁止碰"列表没有 X，就是不一致。两边必须互相印证。

4. **ADR 触发条件是否无歧义？** — 每个触发条件必须能用"是/否"判断。模糊条件（如"重要的设计决策"）会导致该创建时没创建、不该创建时创建了。

5. **回滚策略是否覆盖代码和数据两个维度？** — 只写 git revert 但没写数据回滚 = 不完整。回滚触发条件必须有明确的时间或状态阈值（如"1 小时内无法修复"），不能是"必要时回滚"。

---

## project-init-template review

**核心准则**：foundations/project-init-template.md 是给**人**看的操作手册——项目初始化者在配置新项目时需要的完整指南（模板、章节说明、目录初始化命令）。读者是人，不是 Agent。Skill 在激活时由系统加载此文件并代为执行初始化，但文件本身面向人的阅读理解。

> 项目接入的治理规则（前置条件、触发流程、约束边界）见 governance/project-init.md。

### 自查问题

1. **读者是人还是 Agent？** — 如果是给 Agent 看的执行指令（如"Skill 被激活后先读取 project-basic.md"），应放在 methodology.md。foundations/project-init-template.md 的内容应该是"人在创建新项目时需要知道什么"。

2. **模板是否完整？** — 按模板从头到尾填写，能否覆盖一个项目所需的全部元信息？缺失的章节应在模板中补充。

3. **初始化命令是否可直接执行？** — shell 命令块中不能有 `${placeholder}` 等需要人工替换的变量。命令必须是复制粘贴即可运行的。

4. **章节必填/按需的标注是否明确？** — 模板中每一章都应该在章节说明表中标注必填还是按需，人对"这个要不要填"不应存疑。

5. **功能模块开关的说明是否足够做出决策？** — 人读完后应该能判断：我的项目该用目录隔离还是分支隔离？该启用安全测试吗？

---

## project-init review

**核心准则**：governance/project-init.md 定义项目接入的治理规则——前置条件、触发流程、约束边界。读者是全体 Agent 和用户。它回答"接入的条件和边界是什么"，不回答"具体怎么操作"（操作模板见 foundations/project-init-template.md）。

### 自查问题

1. **治理规则与操作模板的边界是否清晰？** — project-init.md 定义"什么能做、什么不能做"，project-init-template.md 定义"怎么做"。如果 project-init.md 中出现了 shell 命令、模板填写说明、盘点步骤等操作指令，应下沉到 template 中。

2. **三种接入场景的治理是否完整且对称？** — 🆕 全新项目、🔧 全新项目（无 Git）、📦 存量项目接入——三种场景的治理原则是否覆盖了各自的特殊步骤（🔧 的 git init、📦 的盘点）？是否有场景被遗漏？

3. **约束与边界是否与 methodology.md 一致？** — "初始化必须在任何 Agent 工作之前完成"与 methodology 的 Agent 启动顺序是否一致？"功能模块开关一经设定不应频繁变更"与 methodology 的功能模块章节是否呼应？

4. **📦 存量项目的额外约束是否足够保护已有资产？** — 非破坏性合并、描述现状不预设未来、版本号尊重历史、存量数据视为生产数据、Git 历史完整保留——这五条是否覆盖了存量接入的所有风险点？缺少任一条则存量项目的数据或历史面临风险。

5. **初始化后的状态描述是否与目录结构一致？** — project-init.md 描述的初始化后项目状态（标准目录、核心 5 环境、可选环境）是否与 methodology.md 的标准目录结构完全对应？任一方的增删必须同步到另一方。

6. **触发条件是否无歧义？** — 用户表述模糊时（如只说"帮我建个项目"），治理规则是否要求主动追问澄清场景？如果不要求，Agent 无法判断走哪个初始化路径。

---

## Agent reference 通用准则

以下准则适用于 `agent-analyst/`、`agent-planner/`、`agent-deliverer/`、`agent-qa/`、`agent-pmo/` 下所有 Agent reference 文件。

**核心准则**：Agent reference 是给 Agent 执行时查阅的"操作手册"——定义该 Agent 的完整工作流、每一步的输入输出、门禁检查清单。读者是 Agent，不是人。内容必须可执行：Agent 读完后不需要猜测任何步骤。

### 通用自查问题

每个 Agent reference 文件必须逐一通过以下检查：

1. **角色定义一致性** — 角色定义表（可写/只读/禁止碰）与 methodology.md 职责边界表是否逐项一致？任何一方的增删改必须同步到另一方。

2. **流程完整性** — 是否覆盖了该 Agent 涉及的所有场景？（标准通道 + 快速通道 + Hotfix + 按需触发 + 用户直接指令）。缺少任一场景 = Agent 遇到该场景时无章可循。

3. **输入输出明确性** — 每一步是否明确了：（a）从哪个目录/文件读取什么数据？（b）产出写入哪个目录/文件，什么格式？Agent 不应猜测路径或格式。

4. **前置条件** — 文件开头是否声明了 Agent 启动前应确认的状态？（需读取哪些文件、确认哪些目录存在、检查哪些信号）。

5. **门禁可执行性** — 门禁检查清单中每一项是否可逐项打勾？必须是布尔条件（"XX 已填写"、"XX 已通过"），不能是模糊描述。

6. **引用不重复** — 是否引用了 methodology.md 或 governance/design-principles.md 定义的规则而非重复定义？例如"遵循设计原则"是引用，"最小变更——只改受影响部分"是重复。

7. **步骤编号连续性** — 步骤编号是否连续，无跳跃？编号跳跃（如 4.7 后直接 6.1）必须有明确说明。

---

## agent-analyst/requirements-analysis.md review

**定位**：需求分析Agent 的唯一操作手册。覆盖需求澄清 → 方案设计 → 分流决策的完整链路，以及 backlog 条目的创建与精炼。产出 `requirements/backlog/BL-XXX.md` 和 `define/`（含 ADR）。

**专属自查**（在通用准则基础上追加）：

1. **backlog 条目格式是否自足？** — 模板中的 frontmatter 字段和章节必须能让 Agent 无歧义地填写。每个字段的含义、必填/可选、填写时机（draft/refined/ready 哪个阶段填）必须在字段说明表中覆盖。

2. **分流决策条件是否可机械判定？** — "变更文件数 ≤ 2"中"变更文件"指的是源码文件还是包含 define/data？判定条件中的每个术语必须有精确的计数范围。

3. **精炼触发时机是否覆盖了所有该触发的情况？** — 如果用户补充了想法但 backlog 条目仍是 draft，是否触发精炼？如果用户说"那个需求再想想"，Agent 应如何响应？

4. **门禁 1 和门禁 2 的检查项是否有重叠或遗漏？** — 门禁 1（需求确认）的检查项是否覆盖了 1.1-1.6 的所有产出？门禁 2（方案确认）是否覆盖了 2.1-2.5 的所有产出？

---

## agent-planner/version-planning.md review

**定位**：规划Agent 的唯一操作手册。覆盖从需求池选取 → Goal→Epic→Story 拆解 → 版本划分 → Plan 文件输出的完整链路。产出 `plan/to-do/`、`versions/`、回写 `requirements/requirements.md` 和 `requirements/scenario.md`。

**专属自查**（在通用准则基础上追加）：

1. **选取流程是否给人留了决策权？** — 规划Agent 不能自动决定选哪些条目。选取步骤是否明确了"向用户展示 → 用户确认 → 执行"的交互模式？

2. **Story 拆解标准是否可操作？** — "从用户视角出发，描述一个端到端的操作流程"——Agent 据此能否判断两个 Story 该合并还是该拆分？需要反例（什么不是好的 Story）。

3. **版本划分的客观性** — 分版本的判定条件是否有明确的数值阈值？"Story 数量 ≥ 3 个"是可判定的，"变更太复杂"不是。

4. **输出文件格式是否完整？** — Plan 文件模板、roadmap 格式、v{x.y.z}.md 格式、requirements.md 格式、scenario.md 格式是否都给出了完整可复制的模板？模板中的占位符是否都有对应说明？

5. **场景编号分配规则是否明确？** — S-NN 编号跨版本延续，但"在何处维护当前最大编号"是否说明？

---

## agent-deliverer/implementation.md review

**定位**：交付Agent 的唯一操作手册。覆盖双轨道（标准通道从 `plan/to-do/` 拉取、快速通道从分流决策接手）的代码实施、自审、移交 QA、记录发布的完整链路。

**专属自查**（在通用准则基础上追加）：

1. **双轨道的入口判断是否无歧义？** — Agent 如何区分"标准通道"和"快速通道"？信号列表是否覆盖了所有可能的激活场景？

2. **快速通道和标准通道的自审清单是否一致？** — 两条通道的自审标准不应有差异（代码质量不因通道而降低）。如果快速通道少了检查项，必须有理由。

3. **移交 QA 的契约是否完整？** — QA Agent 收到 `plan/verify/` 中的文件后，是否包含了验证所需的所有信息（测试用例编号、验收标准、变更范围）？

4. **回滚操作是否覆盖了代码和数据两个维度？** — 回滚步骤是否包含了数据从 `.backup/` 还原的操作？是否说明了回滚后如何通知上下游？

5. **步骤编号跳跃是否有说明？** — 4.7 之后直接跳到 6.1（5.x 是 QA Agent 的步骤），是否在上下文中清晰解释了编号规则？

---

## agent-qa/quality-assurance.md review

**定位**：QA Agent 的唯一操作手册。覆盖双轨道（标准通道按 Story 验证、快速通道合并验证）的独立验证、环境管理、安全/压力测试、质量门禁判定的完整链路。

**专属自查**（在通用准则基础上追加）：

1. **独立性的保障机制是否明确？** — QA Agent 的"独立"体现在哪里？（不读源码实现、不修改源码、只看 Plan 文件和测试用例）。如果 QA 发现失败，反馈路径是什么？

2. **环境管理的权限边界是否明确？** — QA Agent 只在 `envs/qa/` 可写。其他环境的操作权限（读/写/禁止访问）是否逐环境标注？

3. **安全/压力测试的触发条件是否可机械判定？** — "涉及认证/授权逻辑变更""涉及数据库查询路径变更"——Agent 如何判断？是从 Plan 文件的描述中判断，还是从变更文件清单中判断？

4. **质量度量是否与 methodology.md 的度量指标一致？** — methodology.md 定义了三个指标（测试通过率、场景覆盖率、变更失败率），qa/metrics.md 的记录模板是否覆盖了 QA Agent 应记录的所有指标？

5. **失败反馈的闭环是否完整？** — 验证失败 → 通知交付Agent → 修复 → 重新验证。这个循环中 QA Agent 的操作（移回 Plan 文件、附失败报告）是否每一步都明确了？

---

## agent-pmo/reporting.md review

**定位**：PMO Agent 的唯一操作手册。覆盖数据源扫描 → 看板生成 → 异常标记的完整链路。PMO 是纯观察者——只读全项目数据，只写 `pmo/dashboard.md`。

**专属自查**（在通用准则基础上追加）：

1. **只读约束是否贯彻全文？** — PMO Agent 的"禁止碰"列表在 methodology 中定义，在 agent-pmo/reporting.md 的角色定义中必须逐项一致。看板生成流程中的每一步是否都确保了只读？

2. **数据源映射是否完整？** — 看板模板中的每一个数据项，是否都能追溯到明确的数据源文件和提取方式？如果某个数据源文件不存在（如项目刚初始化尚无 `change-log/`），Agent 应如何处理？

3. **异常标记规则是否可机械判定？** — 每个异常条件的阈值（如"WIP > 6""僵死 > 30 天""积压 > 3 且 > 24h"）是否明确？Agent 不需要主观判断"是不是太多了"。

4. **看板模板是否可增量更新？** — PMO 有两种触发模式（版本发布后全量刷新、按需增量更新），模板和流程是否说明了增量更新时哪些章节应保留、哪些应重算？

5. **主题进度计算是否可操作？** — "按 initiative 聚合"、"统计各版本 Story 完成情况"——计算步骤是否足够具体？Agent 如何获取"已完成 Story 数"（从 `plan/done/` 统计还是从 `versions/` 反推）？
